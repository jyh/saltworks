CORRECTION (see 4th commit after this one): this commit's message and the file header it shipped both claim the uniform 32x32 RegNext statement was absent from the kernel and that run_ssa_gate was the first tool able to prove it. BOTH FALSE -- SaltWorks.Stack.Program.run_regNextN (Program.lean:7476) already proved it, landed and audited. The general tool run_gate_val is genuinely new; the motivating absence claim was not true. Corrected by compiler seat 08/18.
