# COUNCIL PACK — 2026-08-10 16:00 (may slip to 17:00 at the Captain's word)

**Maestro (Fable hand, the day-5 successor). Agenda as registered
(QUEUE:1078-1087) + the trio. Items ordered for decision density.**

## ① THE AIM-HIGH TRIO (the sitting's core)

Three scoping blocks, drafted this sitting, committed at `1988c36`,
refuter-passed before this pack was finalized (verdicts + amendments: §①a):

- **Block ① language** — docs/aim-high-language-v1.md. Star: a COMPLETE
  verified compiler (correctness + completeness) for tiny-Rust with `while` +
  functions, closing with the Batcher sort recompiled from source. Key
  design move: Row A in ∃-fuel form over the EXISTING `runFor` decouples
  `while` from the B-EXEC driver row.
- **Block ② executive** — docs/aim-high-executive-v1.md. Star: a verified
  multitasking executive — preemption by fuel quantum at the spec level,
  fairness over infinite runs (step-indexed driver decision made in-block),
  isolation as a COMPILER-ENFORCED register-partition theorem (your
  "guaranteed isolation" steer made the spine). Runs at math's seat,
  PARALLEL to block ①. "OS" stays fenced; X5 names what would earn it.
- **Block ③ application** — docs/aim-high-application-v1.md. Star: a
  verified layer-compiler for message-passing networks, demonstrated on a
  **GraphCast-lineage MESSAGE-PASSING STEP** (your 11:4x steer, T1-logged)
  at PoC scale — 4-node mesh, int8, in-band weights, SYNTHETIC dynamics
  (no weather data enters; no forecast skill is measured or claimed) —
  plus ONE COMPILED ADJOINT round (the verified-AD flagship at the layer
  level, where it is honest). Firewall kept as the promotable drawer
  resident; self-hosting drawered with its price tag.

**Decisions asked of the Captain:** the veto points listed per block (§7 of
each), plus the ONE cross-block call only he can make: sequencing at
compiler's contested seam (blocks ① and ③ + W5-asm + B-ISA cannot all ride
one write track in nine days — the refuter-sequencing verdict quantifies the
overrun and the largest honest bundle).

### ①a · REFUTER VERDICTS + AMENDMENTS FOLDED (pass complete 12:0x; 5 refuters, 166 tool calls over the actual corpus)

**All five: REPAIR-THEN-FIRE. Four fatals found; all four repaired or
rescoped in the v1.1 amendment banners (§A of each block) before this pack
was finalized.** The fatals, in one line each:

1. Block ②'s task-table type (`Vector St N`) made the isolation theorem
   vacuous BY TYPE — repaired to ONE shared register file + per-task pcs
   (which also makes the context switch fit dmem8: 4 words vs 60).
2. Block ③'s A4 (multi-round gradient round-trip) caps the seed gradient
   below 1 inside int8 — an identically-zero adjoint no mutant could
   falsify — rescoped to ONE adjoint round with a stated seed bound.
3-4. BOTH seat tallies bust the window: the trio as drafted totals ~18
   compiler-days and ~13.5 math-days against nine, and blocks ① and ②
   each silently claimed the whole math seat.

**Two finds in the GOOD direction:** the fuel algebra block ① proposed to
build is already LANDED and audited (`runFor_add`, `runFor_eq_of_halted` —
Stack/Program.lean), and the refuters confirmed the §4 driver-decoupling
claim itself; and the planned `RegOk` injectivity hypothesis was caught as
pigeonhole-UNSATISFIABLE before any executor burned a day proving green
vacuities.

**THE LARGEST HONEST NINE-DAY BUNDLE (the refuter-sequencing verdict,
maestro-endorsed as the recommendation):**

```
COMPILER  block ① L0 → L1 → L2 → L4   ("a verified compiler exists")
MATH      block ② X0 → X1 → X2        (driver · isolation · fairness)
          + a NAMED salt allocation (≥2 days — the fourth pillar gets
            days or the cut is ruled out loud)
SILICON   block ③ A0 only             (owed V10 work — credit, not capacity)
```

**NAMED AS CUT (ruled, not assumed):** ① L6 the flag (no seat exists for
it; also incompatible with ②'s partitions — sortProg needs 9 registers) ·
① L3/L5 (or ②'s X-ladder dies instead) · ③ A1-A5 (September/v2 claims —
the GraphCast-lineage sentence is NOT a nine-day claim) · ② X3-X5 (X3 was
already veto point (d); X4/X5 already outside).

**RULINGS ONLY THE CAPTAIN CAN MAKE (the pack's ask-list):**
(a) W5-asm + B-ISA deferral — both his standing orders; without deferral,
block ① shrinks to L0-L2. (b) Ruling #3 lift for X0-X2 as spec-level rows.
(c) Partition count N — recommended N=2 (7 regs/task; N=4 leaves 3 and
math's own witness program does not fit). (d) RULING #8 re-open, branch
(A) threshold-network-honestly-named vs (B) the ~40-gate requantizer that
turns h≤127 into a theorem — ruled TOGETHER with the adjoint convention.
(e) L6's fate (default cut; stretch only behind (a)). (f) The salt-pillar
allocation. (g) Block ①'s Row A amended form (the `emit_runs` shape).

**PRE-COUNCIL SEAT SIGN-OFFS (12:1x):** compiler checked block ①'s ladder
against the corpus and DECLINES TO REPRICE (honest as scoped). Evidence
FENCE-PASSED the pack and all three blocks (every raw phrase hit was a
block citing the fence, not breaking it) and PRE-REGISTERED F6 — the
three-tier "verified learning" earn criterion with falsification
conditions and riders (`dec7872`, in the fence doc) — answering block ③
C6's ask before rung A5 exists, which is the only time it is worth
anything.

## ② SALT NEXT STEPS

- Pi submission (the ratified venue; flagship skeleton at papers/flagship/).
- arXiv endorsement effort — REOPENED ACTIVE at his word.
- Clearance status: salt RE-APPROVED ✓ · jas APPROVED ✓ · SaltBench PENDING.

## ③ LTI MEETING PREP (Wednesday 8/12)

The narrative REDONE post-meeting. The sheet + the fact file are in
${LOCAL_SEAT}/. **THE DELTA — what changed at the meeting — MUST COME
FROM THE CAPTAIN at this sitting (ask first, per the bank).**

## ④ FAB LOGISTICS TAIL (status, no asks beyond the clicks themselves)

- **TT CI: FULLY GREEN on the live commit as of 11:5x** — gds ✓ gl_test ✓
  precheck ✓ docs ✓ test ✓ viewer ✓ (the viewer needed the duplicate
  Pages-artifact fix: both artifacts deleted, attempt-3 SUCCESS; the
  hardening was green throughout).
- The three clicks remain HIS: submit NDF at app.tinytapeout.com · upgrade
  2x2→6x2 · buy the 1x2. Evidence's PUBLIC-BYTES read: registered.
- Tile plan confirmed 10:4x (1x2 + 6x2, ≈€910, Bellcore full-BB via the
  update window). PCBs 0/80: resurface with the B5 fact sheet only.
- Story-completers: the partial-load lift is COMPLETE — (A)(B)(C)(D) all
  landed at `819c685` (+ the key level `b359571`, rename `4151767`),
  silicon MEAS no-defect (11:43, 11:58), math NOT REFUTED. Carried with
  1b5453c's own guard: this is NOT "the Batcher sorts" — the
  abstract-fold↔cSorted seam remains, abstract to abstract, and math has
  it measured at one obligation. Minimal-executive's driver row is block
  ②'s X0. The Captain's "easy and completes the story" pair is at the door.

## ⑤ FLEET STATE ONE-LINER

First seal `02155be` (BatcherRun + PortLengths rooted; closure ledger clean —
disk-vs-graph now returns only Scratch*). The phantom-HALT affair resolved
fleet-side with three watch fixes and a struck overstatement, all banked by
their owners. Watches: bus v2 + sentinel + modelwatch + bus-sync alive.
