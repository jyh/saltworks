# SEAM-A RE-SCORE — VERDICTS

**Scored 2026-08-15 13:5x–14:0x against the bar fixed in
`compiler-seamA-rescore-PREREG-0815.md` (`5de0269`), written before any row was read.**
Criteria pinned at `1c52970`; before-state pinned at `cf1aab8`.

All four live rows were scored before any verdict was written, per the pre-registered bar
(row-at-a-time writing lets an early call anchor the later ones).

## THE DELTA — BOTH NUMBERS NAMED, per the ruling

```
                BEFORE (cf1aab8)        AFTER (this file)
39254           EXCLUDED-G1             EXCLUDED-G1      no move
56338           EXCLUDED-G2             EXCLUDED-G2      no move
58129           IN                      EXCLUDED         ⬅ MOVES
64022           IN                      IN               no move, now RULED
64680           EXCLUDED-G3             EXCLUDED-G3      not a live seam (see PREREG §4)

aggregate over the five named specimens
  BEFORE   IN = 2   EXCLUDED = 3
  AFTER    IN = 1   EXCLUDED = 4
  ONE ROW MOVES.
```

## Per row, with the rule line each turns on

### `39254` — EXCLUDED-G1 **HOLDS**
*maestro 08/09 03:22; 2 lines, 839 chars. A design sketch gains §6/DESIGN B, announced for
the morning; its own words are "Nothing fires."*

The seam was: *it carries a checkable mathematical assertion incidentally (compare-exchange
= max/min = tropical semiring; ReLU nets tropical-rational), and no rule covered an exhibit
riding inside a prospective frame.*

**RULE LINE — `:196` §1.5 REPAIR A:** a finding fires a class test only if it is
*instantiated, live, and in the row's own subject matter*; **prospective, averted,
prescribed and historically-cited findings never fire.**
**RULE LINE — `:372`:** gating in requires *"an instance of the thing being done wrong."*

⇒ **The seam dissolves rather than resolves: a checkable mathematical ASSERTION is not a
FINDING OF A FAULT.** No instance of anything being done wrong is exhibited, so the frame
question never arises. §2.2(a) is not reached — none of EXHIBIT / CITATION / CONFESSION
applies, because the row asserts no defect of any artifact.

### `56338` — EXCLUDED-G2 **HOLDS** *(and agrees with the governing doc by a second route)*
*math 08/10 15:59; 18 lines, 2,246 chars. A liveness post listing discharges.*

**RULE LINE — `:196` §1.5 REPAIR A**, as above. The discharges it lists are **PASSES**, and
a disclosure of how a pass was established is not a fault. No instance exhibited.

⇒ **Independently, the codebook already rules this row off the ladder** at §3.1(5):
*"'measured fact' EXCLUDES an unmeasured self-assessment (56338, 65312, 88789 → off the
ladder)."* Two routes, same place. ⚠️ **This row is on the codebook's own "eight the
ratifying sitting should re-read BY HAND" list**, described there as the line *"where the
next two coders are most likely to disagree"* — so this verdict is offered as that hand
re-read, and a second coder disagreeing is an anticipated outcome, not an anomaly.

### `58129` — **IN → EXCLUDED. THE ONE MOVE.**
*silicon 08/10 18:25; 380 lines, 16,411 chars — the largest post in the pool.*

*Extent verified before scoring: zero fence markers and zero header-shaped lines inside the
region, and it closes on the author's own close-line at `:58507` with the next post at
`:58509`. The Verilog and build output inside it are pasted artifacts, not other posts.*

The original basis gated it IN because *"the pre-commitment is exhibited BY this post
(exhibit form under §2.2(a))"*. The pre-commitment is at `:58505`:
> *"I will report the measured cost either way and I will not editorialise a slow result
> into a blocker."*

**RULE LINE — `:334` §2.2(a), EXHIBIT arm:** *"the artifact is THIS POST … **it fails** →
GATED IN."*
⇒ **The operative word is FAILS, and this post fails nothing.** It CLAIMS a node, states a
reason, pre-commits on reporting posture, and reports `saltbuild EXIT=0`. A pass is not a
fault — the same reading that keeps `56338` out.
**RULE LINE — `:196` §1.5 REPAIR A:** the pre-commitment is about FUTURE reporting and the
M0 measurement is still pending. **Prospective. Never fires a class test.**

⇒ **EXCLUDED.** *Exhibiting a COMMITMENT is not exhibiting a FAILURE, and §2.2(a)'s exhibit
arm requires the artifact to fail.*

### `64022` — IN **HOLDS**, and the recorded disagreement now RESOLVES ON THE RULING
*silicon 08/11 05:50; 11 lines, 1,545 chars. Three minutes past its own deadline, cause
pre-announced at 05:20.*

**BOTH READINGS, printed as the bar requires:**
- **MINE (at sweep):** the row asserts a fault of its own subject matter — three minutes
  past its own deadline — so under R-D it is not a specimen, under REPAIR A it sits in the
  row's own subject matter, and `:372` gates it in.
- **THE HELM'S (19:33 refutation, point 4):** the incidental content is *"a pre-announcement
  paying off"* and therefore NOT A FAULT; gating it in would admit a non-defect row to a
  defect ledger.
- **The disagreement, stated then:** *whether a breach whose cause was pre-announced is
  still a breach.*

**RULE LINE — `:334` §2.2(a), CONFESSION arm:** *"the artifact is the AUTHOR'S EARLIER ACT …
**it fails** → GATED IN, **WITH THE EVENT LOCATED AT THE EARLIER ACT** (RELOCATION)."*

⇒ **§2.2(a) as written carries no exemption for a predicted failure.** The deadline was
missed; the artifact failed. **And the RELOCATION clause is exactly the protection the
helm's objection reaches for** — it exists so that *"confessing costs a seat nothing"* and
so the corpus does not *"generate rows in proportion to a seat's honesty."* The concern is
answered by relocation, not by exclusion.

⇒ **IN HOLDS — now on a rule rather than on a hold — with the event LOCATED AT THE EARLIER
ACT (the missed 05:46 deadline), not at `64022`.** *That relocation is a change to the
row's bookkeeping even though the verdict does not move, and it is stated here rather than
left implicit.*

### `64680` — not a live seam; before-state stands
*Its contest was already resolved under R-D at sweep time (`contested=no`, empty seam
field). Re-read: the resolution holds — the row IS a null result and says so, and the
observability argument is the RULE THE ROW IS MAKING, so under R-D it does not gate the row
in. **No change, and it should not have been on the commission's list.***

## What this does NOT settle

- **B-2's coverage number** moves with `58129` leaving `IN`. That recomputation is B-2's
  work and is not performed here.
- **`56338`** is a thin call by the codebook's own admission. A second coder disagreeing
  should be read as the codebook's prediction coming true, not as this pass being wrong.
- **`64022`'s relocation** changes where the event is located. Whether the ledger's existing
  entry needs moving is a bookkeeping question I have flagged and not executed.
