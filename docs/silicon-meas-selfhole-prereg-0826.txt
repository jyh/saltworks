PRE-REGISTERED PREDICTION — silicon, 2026-08-26T23:01:05-0700
Subject: meas_since.sh manufactures its OWN reds inside a single sweep.

MECHANISM CLAIMED (sharper than the banked "landed after my last build"):
  meas_build.sh's PATH form kernel-checks a target and WRITES NO OLEAN BY DESIGN.
  A MEAS range is ordered by git history, NOT by import dependency. So when file B
  in the range imports file A also in the range, A is checked GREEN, no olean is
  written, and B then REDS for want of A's olean. The sweep digs its own hole.

DISCRIMINATING EVIDENCE ALREADY IN HAND (range d3190e8f..c19d12f0):
  DecoderLines.lean was KERNEL-CHECKED GREEN by THIS sweep (4s, EXIT=0)
  and .lake/build/lib/lean/SaltWorks/HDL/DecoderLines.olean DOES NOT EXIST.
  The banked history-based story cannot explain that; the path-form story does.

PREDICTION, MADE BEFORE THE SWEEP REACHED THESE FILES:
  MemWiring.lean       -> RED, and the error names a MISSING OLEAN (build state),
                          for one of: AdapterPlacement / DecoderLines / MemOrganPlacement
  StallsAtWidened.lean -> reaches the kernel (no missing import olean)

BAR: the prediction is REFUTED if MemWiring reds for any reason OTHER than a
missing olean, or if it goes green, or if StallsAtWidened reds on build state.

DETECTOR CONTROL (run before the prediction, both branches reachable):
  MemOrganPlacement.lean -> names SaltWorks.HDL.AdapterPlacement  (positive)
  DecoderLines.lean      -> empty                                  (negative)

---
REGISTERED FOLLOW-UP (not built — meas_since.sh is EXECUTING as I write this, and editing a
running `sh` script is its own hazard; this is registered, not reactively patched):

The pre-flight builds each missing olean with a SEPARATE `saltbuild.sh <module>` call, and each
call acquires the machine-wide fleet flock independently. For MemWiring the missing set was
{AdapterPlacement, DecoderLines, MemOrganPlacement} = THREE lock acquisitions, each queueing
behind every other seat. Building the TARGET's module form once resolves all deps transitively
in ONE acquisition.
  Correctness note, checked before proposing it: populating the target's olean via the MODULE
  form does NOT weaken the witness, because the PATH form that follows elaborates the target
  regardless (meas_build's own table). This is exactly the manual two-step my bank prescribed.
  It is an EFFICIENCY change under contention, NOT a correctness fix — stated that way so a
  successor does not price it as the latter.

MEASURED CONTEXT, 08/26 23:37: a peer held the flock 35 minutes on Salt.MR.All (verified
PROGRESSING, not wedged: ~200% CPU across two samples, 1 lean proc, 6.7 GB resident vs the
43 GB hazard). Under that kind of hold, 3 acquisitions vs 1 is the difference between one wait
and three.
