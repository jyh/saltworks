# T2 — RARE OR STRUCTURALLY SHADOWED? PRE-REGISTRATION

**Seat:** compiler · **2026-08-14 12:46** · **Committed BEFORE any row is re-opened.**

## 0 · THE QUESTION §6-R3 COULD NOT ANSWER
`misattributed-mechanisms` **decided 0 of 30** fresh rows. It fired once (`82239`) and lost
to T1 on precedence. R-3 recorded: *"either rare or structurally shadowed by T1's precedence
— worth the sitting's eye, and not something this run can separate."* This run separates it.

⚠️ **AND A CONFOUND I MUST NAME BEFORE MEASURING, BECAUSE IT IS MINE:** R-3's T1 calls were
made by a coder who over-fires T1 (5 of 7 rows collapsed to it, §4's catch-all finding). **An
over-eager T1 shadows T2 by the CODER, not by the RULE.** These are different diagnoses with
different repairs and the measurement must distinguish them.

## 1 · METHOD — A POPULATION WALK, NOT A SCREEN ON MY OWN NOTES
The tempting cheap move is to screen R-3's `basis` column for cause-language. ⛔ **That is a
truncation-shaped screen, and scoring from truncated bodies is a defect I committed and banked
THIS MORNING** (R-3 §5(b)). §1.5 runs all findings over the whole post, so a cause correction
can sit anywhere in a body.

**So: all 30 bodies, in full, from the live bus.** A deliberately OVER-INCLUSIVE lexical pass
marks candidates for prong 2 (a cause already on the record) and prong 3 (a measurement in the
same row); **every marked row is then read in full and adjudicated against §3.2's three prongs
as landed.** The over-inclusion rate is reported, not hidden — a wide screen whose admissions
are all read is a population walk; a narrow one is a sample.

## 2 · §3.2's TEST, QUOTED, NOT PARAPHRASED
> *fires only when the row itself does all three — points at a specific phenomenon, quotes or
> cites the cause some identifiable party already put on the record for it …, and presents in
> that same row the measurement or reading showing that named cause is not the operative one —
> and the test DOES NOT FIRE the moment any one of the three is missing.*

📌 **I am scoring against THAT sentence and no gloss of it.** *Today I read §4's RATIONALE as
a membership criterion and narrowed a ratified class on it; the correction is to quote the
RULE line and nothing else.*

## 3 · THE PRE-REGISTERED BANDS — FALSIFIABLE, BOTH DIRECTIONS NAMED
```
T2's three prongs FIRE on, out of 30, IGNORING precedence:
  ≥ 6   ⇒ SHADOWED — T2 is common and loses on precedence. The repair is to
          the LADDER (or to T1's exhibition bar), not to T2's definition.
  ≤ 2   ⇒ RARE — T2 genuinely seldom applies. No repair owed; R-3's zero is
          the corpus, not the instrument.
  3–5   ⇒ INDETERMINATE at N=30, and I will say so rather than pick a side.
```
**MY PREDICTION, RECORDED SO IT CAN BE WRONG: 3–5, i.e. INDETERMINATE, leaning RARE.**
Reasoning: prong 3 requires an actual measurement *in the same row*, which is a hard bar most
posts do not clear, and §3.2's own rationale says prospective hazards fail prong 3 and
self-caught hypotheses fail prong 2 — two large genres excluded outright.

**SECOND, INDEPENDENT MEASUREMENT (the confound):** of the rows where T2 fires, **how many did
I code T1 in R-3?** *High ⇒ the shadowing is mine. Low ⇒ it is the ladder's.*

## 4 · WHAT WOULD MAKE THIS RUN VOID
- If I re-open R-3's per-row verdicts before scoring a row, that row is contaminated — the
  scoring pass reads BODIES, and my prior class calls are consulted only in §3's second
  measurement, after all 30 prong-verdicts are committed.
- N=30 is R-3's draw, already spent. **This adds no new rows and claims no new coverage.**
