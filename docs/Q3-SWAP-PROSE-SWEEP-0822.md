# HORN D SWAP — THE SILENT-SITE SWEEP, 2026-08-22

Four independent read-only lenses over `SaltWorks/**` (hardcoded twins · decided literals · prose ·
controls and consumers), each finding adversarially verified by a separate agent prompted to REFUTE
it and to default to refuted when uncertain. **40 agents, 0 errors, 19 rows survived, 17 refuted.**

⚠️ The refuted 17 are the reason to trust the 19: most died on the SILENCE criterion — the claim was
factually right and the build catches it loudly, so it is not a silent site. A sweep that confirmed
everything it found would be measuring its own enthusiasm.

⭐ FIVE of the six `StateCodec.lean` prose rows were ALREADY FIXED by the staged patch before this
sweep reported; the sixth (`:234`) was fixed on reading it. The rows below are the REMAINDER.

| file:line | kind | what the swap does to it |
|---|---|---|
| `SaltWorks/HDL/CoreOffsets.lean:92` | HARDCODED | Must become 1345. NEW MEASUREMENT beyond the runbook: NOTHING outside this file touches `off0`/`offsets`/`chain_last`. `git ls-files SaltWorks/**/*.lean \| xargs grep -n CoreOffsets` returns only PROSE mentions (AccountMeasure.lean:22,32,33,75 and CorePlace.lean:18,23,30,1047,1082); AccountMeasure.lean:6 imports only SaltWorks.HDL.CorePlace, so it cannot reference CoreOffsets even if it wanted to.  |
| `SaltWorks/HDL/StateCodec.lean:157` | PROSE | Inverts under D and, worse, ARGUES AGAINST the swap from inside the function being swapped (lines 158-159 continue '…extending `encD` to 1313 bits would reopen the flop budget'). MEASURED HAZARD: if the executor widens `stWidth`/`stBit`/`encD` and leaves `decQ`'s body unchanged (`mem := Vector.replicate 8 0` :160, `trapped := false` :161), the file STILL ELABORATES — `decQ_encD_proj` (:178) only c |
| `SaltWorks/HDL/RegFieldSchema.lean:143` | PROSE | `1088 = coreInWidth` becomes FALSE (coreInWidth = 1345). This is an EQUATION to a derived value, not a passing mention, so the sentence is wrong rather than merely stale. The theorem below it (`core_state_bit_stable`, :144) re-proves green: its proof is `simp only [coreInWidth, stWidth, Net]; omega`, which discharges `32r+k < 1345` just as happily. |
| `SaltWorks/HDL/C4.lean:128` | CONTROL | THE HIGHEST-VALUE ROW. `coreShort_is_rejected` (C4.lean:137, `by decide +kernel`) KEEPS PROVING after the swap — 1055 ≠ 1313 just as 1055 ≠ 1056 — so the build is silent. But the control was built as the OFF-BY-ONE fixture (its own docstring, C4.lean:125-126 and :136: "emits 1055 outputs instead of 1056", "1055 against 1056, well-typed either way"). After the swap it is off-by-258 and no longer te |
| `SaltWorks/HDL/CoreOffsets.lean:92` | HARDCODED | → 1345. The runbook's manual class 1, and I can now bound its silence exactly: NO theorem anywhere in the tree relates `CoreOffsets.off0` to `StateCodec.coreInWidth`. `command grep -rn CoreOffsets SaltWorks/` returns only PROSE mentions (AccountMeasure.lean:22/32/33/75, CorePlace.lean:18/23/30/1047/1082). So if `off0` is LEFT at 1088, `chain_closes`, `total_reconciles`, `total_reconciles_against_a |
| `SaltWorks/HDL/StateCodec.lean:44` | PROSE | Must become `1313 bits total = 32 regs × 32 + 32 + 8 words × 32 + 1`, AND the table it closes (lines 41-43) must gain two rows: `1056 … 1311 mem word w, bit k, at 1056 + 32*w + k` and `1312 trapped`. Lines 42-43 (regs 0…1023, pc 1024…1055) are TRUE under D and must NOT be touched. |
| `SaltWorks/HDL/StateCodec.lean:59` | PROSE | Must become "…plus the pc, the eight memory words and the trap flag." ⚠️ THE HIGHEST-RISK ROW IN THIS LENS: it is the docstring ON `def stWidth` itself, it contains NO NUMERAL, and therefore no grep for 1056/1088/34 can find it. The definition one line below changes; this sentence does not. |
| `SaltWorks/HDL/StateCodec.lean:157` | PROSE | Wholly inverted: after the swap `encD` encodes 1313 bits INCLUDING mem and trapped, and the extension has happened. The comment must be deleted or rewritten as history. ⚠️ It sits directly above `mem := Vector.replicate 8 0` in `decQ` — and the runbook (STEP 2b) uses THIS COMMENT as the marker distinguishing `decQ`'s replicate (must change) from `decQtransposed`'s (must NOT). If the line the swap  |
| `SaltWorks/HDL/StateCodec.lean:173` | PROSE | Asserts the EXACT OPPOSITE of the post-swap truth. Runbook STEP 2b ③ proves the full round trip `decQ_encD (s : St) : decQ (fun j => (encD s).getD j false) = s` and DERIVES this projection from it. Must become: "the whole-`St` form is TRUE under D (`decQ_encD`); this projection is now a corollary, kept so no caller breaks." `decQ_encD_proj` still elaborates, so the build is silent while the docstr |
| `SaltWorks/HDL/StateCodec.lean:204` | PROSE | Must become "the hypotheses `hm`/`ht` are DEAD under D — the codec inverts on every `St`, dirty or clean; they are retained only so existing callers keep type-checking." Runbook STEP 2b ③ literally states `decQ_encD_of_clean (s) (_hm) (_ht) … := decQ_encD s -- hypotheses now DEAD`. The docstring will still describe a conditionality that no longer exists, and a reader will keep discharging cleanlin |
| `SaltWorks/HDL/StateCodec.lean:234` | PROSE | `1056-bit` → `1313-bit`. (The subject, `decQtransposed`, must otherwise be left alone per the runbook — only this figure moves.) |
| `SaltWorks/HDL/CorePlace.lean:62` | PROSE | Must become "`1313…1344`, and `coreInWidth = 1345`" — the preceding line 61 also says "state at `0…1055`", which must become "`0…1312`". ⚠️ The same sentence claims the file "cites those rather than restating the numbers" while restating all three; the claim is what makes a reader skip the check. |
| `SaltWorks/HDL/CorePlace.lean:138` | PROSE | `1056 + i` → `1313 + i`. Left as-is it names a formula that is no longer even the wrong-but-plausible alternative, so the warning stops warning about anything. |
| `SaltWorks/HDL/CorePlace.lean:147` | PROSE | → `instrNet i = 1313 + i < 1345`. The proof below it is width-agnostic (`simp only [... coreInWidth, stWidth]; omega`) and will still close, so nothing forces the comment to move. |
| `SaltWorks/HDL/CorePlace.lean:156` | PROSE | → "occupies `1313…1344` and the host's gates begin at `1345`". Sharpest contrast in the file: the docstring's own closing paragraph boasts the theorem was re-anchored on `instrBase + 32` "because THAT is the fact about the input space, and it does not move when the gate space grows" — the THEOREM was made width-agnostic and its preamble was not. |
| `SaltWorks/HDL/C4.lean:128` | HARDCODED | WEAKENS, and keeps passing for a new reason — the dangerous class. `coreShort` is the OFF-BY-ONE control: 1055 against `stWidth` = 1056. `nIn` is derived (`coreInWidth`) but the output count is a bare literal. After the swap `stWidth` = 1313, so `coreShort_is_rejected : ¬ CoreConforms coreShort` (:137) STILL passes by `decide +kernel` — but it now tests that a core 258 outputs short is rejected, n |
| `SaltWorks/Stack/Program.lean:2471` | CONTROL | CONTROL SURVIVES INTACT, ITS EXPLANATION GOES FALSE. `shortBits := (idealBits ins).take (stWidth - 1)` (:2453-2454) is DERIVED, so `cycOfBits_shortBits_pad_dependent` (:2476) re-derives to net 1312 and stays a genuine one-output-short control — good. But net 1312 under D is the TRAP FLAG, not 'the pc's top bit', and :2473 'This is C4.lean's 1055-against-1056 hazard' becomes 1312-against-1313. Noth |
| `SaltWorks/Stack/Program.lean:2988` | CONTROL | THEOREM SURVIVES, THREE NUMERALS AND THE MAGNITUDE OF THE GAP GO WRONG. `length_conjunct_is_necessary` (:3031) is fully derived from `stWidth` and its proofs go through `unfold stWidth; omega`, so it re-proves at 1313 — correctly. But :2988-2991 read 'past index 1055', 'they read `getD` at indices below 1056', and '`encD` has exactly 1056 elements'. Under D the fields are silent about ports 1056…1 |
| `SaltWorks/Stack/Program.lean:1533` | PROSE | FALSE: `decQ` will read `0 … 1312`. The theorem it documents (`decQ_congr`, :1537) is stated over `stWidth` and stays true, so only the numeral rots. `decQ_congr` IS in the Q6 measured break list (its `simp only [..., and_true]` at :1551 stops firing), so a repairer will be standing on this line — and the runbook's STEP 2b tells them to drop `and_true`, not to read the docstring. SaltWorks/Stack/* |
