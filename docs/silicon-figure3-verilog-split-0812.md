# FIGURE 3, THE `.v` ROW — hand-RTL vs generated, MEASURED

**Owed to this hand at the Nature freeze** (maestro, 2026-08-12 14:53): *"the
hand-RTL vs generated `.v` split — raw 316,911/119 files is netlist-dominated,
**the table says so and quotes nothing**."*

## POPULATION — reproduced before splitting it

```
  all *.v in the saltworks repo, .git excluded
    119 files · 316,911 lines
  => EXACTLY the table's raw figure. The denominator is confirmed, not assumed.
```
*Two of the 119 sit outside `SaltWorks/`, and they are counted:
`docs/drafts/tt_um_saltworks_ndf_draft.v` and `docs/hdl-rtl/batcher_net.v`.*

## CRITERION — a machine marker, never a filename

```
  GENERATED  <=>  the string "Generated by" appears in the first 400 bytes
```
⚠️ **Chosen because a name-based split cannot be trusted here: the generated
netlists live under `Flow/` *and* one structural file sits under `RTL/`.** *This
seat has published a wrong table before by joining on names
([[adjacent-object-principle]]), so the classifier reads the file.*
*Measured marker frequencies: `Generated by Yosys` ×46, `Generated by the` ×1.*

## THE SPLIT

| | files | lines | share of lines |
|---|---:|---:|---:|
| **GENERATED** | 48 | 294,232 | **92.8%** |
| **HAND-RTL** | 71 | 22,679 | **7.2%** |
| total | 119 | 316,911 | 100% |

```
  median generated file   2,192 lines
  median hand-RTL file        61 lines
```
✅ **"NETLIST-DOMINATED" IS CORRECT AND NOW HAS A NUMBER: 92.8% of the lines come
from 40% of the files.** *The asymmetry is the point — a generated file is ~36×
the size of a hand-written one, so a raw line count of a mixed corpus measures
the synthesiser's verbosity, not the design's size.*

## THE ONE BOUNDARY CASE, NAMED RATHER THAN DECIDED SILENTLY

`SaltWorks/Silicon/RTL/adder8s.v` (62 lines) matched on a **comment** —
*"Generated by the command in Flow-docs/c3-probe-0807.md"* — not on a tool
banner. It sits under `RTL/`, yet it instantiates **40 sky130 cells directly**,
so it is structural rather than behavioural and the classification stands.

**SENSITIVITY, so nobody has to trust the judgement:**
```
  as GENERATED  294,232 / 22,679   92.8% / 7.2%
  as HAND-RTL   294,170 / 22,741   92.8% / 7.2%
  => the headline does not move. The conclusion does not turn on this call.
```

## WHAT THIS ROW DOES NOT SAY

- **Not a claim about the DIE.** These are repository bytes. The submitted die is
  built by TinyTapeout CI, and `SaltWorks/Silicon/{RTL,Flow}` holds one copy of
  each module *plus* ~9,000 flops of RISC-V work never fabricated — it over- and
  under-counts at the same time.
- **Not a provenance claim.** "Generated" here means *emitted by a tool*, not
  *kernel-emitted*. The kernel-emission lineage is a separate question and is
  answered by the wrapper's instance list, never by a file marker.
