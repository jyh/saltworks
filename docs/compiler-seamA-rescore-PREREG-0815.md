# SEAM-A RE-SCORE — PRE-REGISTRATION

**Written 2026-08-15 12:51, BEFORE any row is re-scored.** Nothing here is a verdict.
The scoring is a separate, later, landed act; this file exists so that act can be judged
against a bar that was fixed in advance.

## 1 · The commission, as ruled

The helm's ruling, taken as written (bus 08/15 06:51:50, my read-back of it):

> **SEAM-A** — no new rule exists or is needed. APPLY §2.2(a) · §1.5 REPAIR A ·
> REPAIR R-D · :372 **as written**.
> ⇒ MINE: re-score `39254 56338 58129 64022 64680` under them, publish the post-sweep
> delta with **BOTH numbers named**.

## 2 · The four criteria, by locator, with the RULE line

Governing document: `docs/helm-doublecode-codebook-amendment-DRAFT2-0813.md` @ **`1c52970`**.
*Membership is decided from the RULE line, never from a gloss — the gloss is shorter and
more quotable, which is exactly why it is what gets re-quoted.*

| # | locator | what the rule turns on |
|---|---|---|
| 1 | `:334` §2.2(a) | EXHIBITS vs CITES — the operative test. RATIFIED 08-14, sitting ruling 1 of 10. |
| 2 | `:196` §1.5 REPAIR A | the finding must ITSELF pass the gate: instantiated, live, **in the row's own subject matter**. Prospective, averted, prescribed and historically-cited findings never fire a class test. |
| 3 | `:215` REPAIR R-D | the specimen seam: a finding is a fault the row asserts of its OWN subject matter; a fault cited as a SPECIMEN of a rule the row is making is coded at the specimen's row with a §2.5 pointer. |
| 4 | `:372` | *"instance of the thing being done wrong is gated in on that instance (§1.5, all findings)."* |

## 3 · BEFORE-STATE, pinned now so the delta cannot be written after the fact

From `docs/compiler-B1-gate-sweep-scores-0814.tsv` @ **`cf1aab8`**, read mechanically:

```
id      verdict        contested  seam
39254   EXCLUDED-G1    yes        SEAM-A
56338   EXCLUDED-G2    yes        SEAM-A
58129   IN             yes        SEAM-A
64022   IN             yes        SEAM-A
64680   EXCLUDED-G3    no         (none)      <- SEE §4
```

## 4 · ⛔ THE SPECIMEN LIST IS STALE BY ONE — found before scoring, not during

**`64680` IS NOT A LIVE SEAM-A SPECIMEN.** Its row carries `contested=no` and an EMPTY seam
field, and its basis records the contest as *already resolved under the very criterion the
helm ruled I should apply*:

> "FLIPPED BY THE ACT-PERFORMED TEST, and its seam-A contest resolves under R-D: the row IS
> a null result and says so — the filter was run and came back CLEAN. … under R-D it does
> not gate the row in."

⇒ **The live set is FOUR: `39254 56338 58129 64022`.** I will still re-read 64680 and report
it, because a specimen discharged by an earlier pass is exactly the row a re-score is meant
to catch if the earlier pass was wrong — but it enters as *already-adjudicated*, and its
before-state is `EXCLUDED-G3 / not contested`, not `SEAM-A`.

## 5 · ⭐ ANSWER-KEY CHECK — the governing doc already speaks to two of them

*Run before item 1, per the rule that a spec may contain its own answer key.*

- **`56338`** — §3.1(5) already rules it: *"'measured fact' EXCLUDES an unmeasured
  self-assessment (56338, 65312, 88789 → **off the ladder**)"*. **AND it appears on the
  codebook's own list of "SINGLE-ANSWERED BUT THIN — the eight the ratifying sitting should
  re-read by hand"**, described there as *"the measured-fact vs asserted-posture line …
  where the next two coders are most likely to disagree."*
  ⇒ My re-score of 56338 is not a fresh call. **It is the hand re-read the codebook itself
  asks for**, and it must engage that ruling explicitly rather than arrive at an answer
  beside it.
- **`64022`** — filed under *"NO TEST FIRES (ladder result; GATE NOT YET RUN on these)"*.
  ⇒ Its `IN` in the sweep and its "gate not yet run" in the codebook are statements about
  **different instruments**; the re-score must say which it is speaking about.
- `39254`, `58129`, `64680` — no position in the governing doc.

## 6 · The bar, fixed in advance

1. **Every verdict cites the RULE LINE it turns on**, by locator, from `1c52970`.
2. **BOTH numbers named** in the published delta, per the ruling: the before-state above
   and the after-state, per row and in aggregate. No single "corrected" figure.
3. **A row that does not move is reported**, with the rule line that keeps it where it is.
   *A re-score that only reports movement cannot be distinguished from one that only looked
   at rows it expected to move.*
4. **Where I disagree with a landed reading, both readings are printed** — 64022's row
   already records a disagreement with the helm, and that must survive the re-score rather
   than be quietly resolved in my favour.
5. ⛔ **NO VERDICT IS WRITTEN INTO THE TSV UNTIL ALL FOUR ARE SCORED.** Row-at-a-time
   writing lets an early call anchor the later ones.

## 7 · What is NOT claimed here

Nothing is scored. No row has moved. The four criteria have been located and quoted but
**not yet applied**. The only findings in this file are §4 (the list is stale by one) and
§5 (two specimens already have positions in the governing doc) — both established by
reading the record, both before any judgement was formed.
