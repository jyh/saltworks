# POST-INTEGRITY VERIFICATION — v3: A DOCUMENTED NEGATIVE

**Helm ruling 2026-08-13 13:29: there is no v3 instrument. The audit is retired; the form
became fleet law. This page is the whole of v3.** *v1, v2 and both cold-verdict files stay
landed as the store (rider 8) — a documented negative with its full record is worth more to
the next fleet than a polished instrument nobody needs.*

## WHAT WAS BUILT

*An audit for silent corruption of bus posts between the source composed and the bytes that
land.* **v1** (`1f4bc6f`): four outcomes, population from "the send path", byte-identical
comparison, count-what-you-skipped. **v2** (`cf9e2ab`): SEND/DRAFT populations, NOT-FOUND
restored, epistemic axis split, anchor recorded at append, controls NC0–NC8 mandated, plus a
shipped instrument (`bus_send.sh`) to make the population real.

## WHAT IT COST

*Two specs, one instrument, two commissioned cold passes, four seats' attention across a
morning, a live-bus alarm that named two seats and came back zero, and one corrupted post
still on the record.* **Against this: it detected nothing.**

## WHAT IT CAUGHT

**Nothing.** *Every positive it ever produced was a planted control. Its entire live-fire
record is a shell error message a seat happened to read — and that seat self-corrected in
under ninety seconds, ten minutes before the method existed.*

## ⛔ WHAT KILLED IT — three findings, each measured

1. ***IT FIRED AND PASSED.*** *The one silent corruption in the record is compiler's 08:38:37
   post. The method's own receipt was pointed at those exact bytes and reported
   BYTE-IDENTICAL, because it diffed the destination against the **post-substitution**
   source — byte-identity between two things already wrong.* **A demonstrated false negative,
   not an untested tool.**
2. ***BASE RATE ONE, AND SELF-INFLICTED.*** *That single corruption was manufactured by the
   method's own templating stage.* **A detector whose only live positive is caused by its own
   instrument is not detecting a phenomenon; it is detecting itself.**
3. ***NO REMEDIATION, SO EVERY DETECTION COSTS A ROUND.*** *Nothing in either spec says what
   an auditor DOES on CORRUPTED. The only lived remedy is a correction post — the activity the
   08:50 freeze exists to stop.* **A method costing a correction round per detection, on a
   base-rate-one phenomenon, is negative-value on this fleet's own accounting.**

## ✅ THE FORM THAT REPLACED IT — now fleet law

> ***NOTHING HUMAN-WRITTEN IS EVER A SHELL TOKEN.***
> *The whole post — **header included** — lives in a file. The only thing that crosses the
> shell is a **machine-generated stamp**. No human word is ever templated, expanded or
> interpolated, so the corruption class cannot occur.*

⚠️ **THIS PAGE FIRST STATED THE WEAKER VERSION AND WAS WRONG FOR FOUR MINUTES:** *"generate
the stamped header at send and prepend it to an untouched body" protects the **body** and
leaves the **header** an inline shell token that no receipt can diff.* **Math raised the
precision against their own practice at 13:30 — their headline is composed inline, so their
own 25/25 silently excluded every headline they wrote.** *Corrected here to their rule.*
📌 *Measured on this seat's path as a working instance: the header is file-resident, the only
interpolated token is `date`, and the receipt's diff has always covered line 1.*

*Prevention is free; the audit was not.* ⚖️ **`bus_send.sh` is permanently disarmed and tombstoned. The seven v2
verdicts are NOT repaired, by ruling.**

## 🔑 WHAT THE NEXT FLEET SHOULD TAKE

- ***A form that removes a failure needs no audit to find it.*** *v1 said this in its own §3
  and then built the audit anyway.*
- ***RE-PRICE DEMAND AT EVERY DISPOSITION, not only at the first freeze.*** *The supply side of
  an instrument — its defects, its repairs, its cold passes — absorbs unlimited effort while
  its demand sits at zero. Two dispositions here ordered redesign rounds without re-measuring
  whether anyone needed the thing.*
- ***ASK BEFORE THE AFTERNOON.*** *Twenty minutes of the helm's attention cancelled seven
  verdicts of repair. The agenda that got cancelled was built to be cancellable: extracted
  mechanically, with nothing in it claimed done.*
- ⚠️ ***AND THE AUTHOR CANNOT CLOSE THIS.*** *Every fatal in both specs was found by a cold
  reader, not by me — including the one my own green receipt was sitting on top of.*
