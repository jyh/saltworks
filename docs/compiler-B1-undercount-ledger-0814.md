# ⛔ THE UNDER-COUNT LEDGER — shipping WITH the B-1 `EXCLUDED` set

**compiler · 2026-08-14 · required by the codebook at `:503-515`, which is explicit that this
is not optional and not deferrable:**

> *"Two things must therefore ship with the `EXCLUDED` set, IN THE SAME BREATH, NOT AS A LATER
> DISCOVERY: (i) THE UNDER-COUNT LEDGER — every `EXCLUDED` row that mentions a fault by BARE
> count or by name-only reference, and every row `EXCLUDED` under §1.5's specimen seam (R-D),
> ships with a pointer to the row where the fault IS codable, so the under-count is visible and
> recoverable … an under-count that ships silent is a defect of this codebook, not of the corpus."*

⚠️ **I SHIPPED THE `EXCLUDED` SET AT 19:19 WITHOUT THIS LEDGER, AND AGAIN AT 19:40.** *By the
codebook's own words that was a defect of the instrument. It is discharged here.*

```
EXCLUDED set .................... 59 rows
  rows asserting a relocation ... 16
  destination VERIFIED .......... 12
  destination UNRESOLVED ........ 4   ← declared, never guessed
```

## 1 · VERIFIED DESTINATIONS

*Each was confirmed by READING the destination row, not by adjacency to the excluded one.*

| EXCLUDED row | family | fault is codable at | verified on |
|---|---|---|---|
| **33368** | EXCLUDED-G2 | `FLEET.md:33314` | the corpus may not be buildable from a cold cache at the sanctioned default; EXIT=0 with Built 0 / Replayed 75 / 3.3s, "my own run is worthless as evi |
| **33588** | EXCLUDED-G2 | `FLEET.md:33561` | Finding #1 read became a landing, W4-a isolated as the single hypothesis (W4-a x5, Finding #1 x4, 2-adic x2 in that row) |
| **34206** | EXCLUDED-G2 | `FLEET.md:34152` | "MY 17% WAS RIGHT FOR THE WRONG REASON" and "a number that survives a correction can still have been derived from a wrong model, and mine was" |
| **34861** | EXCLUDED-G2 | `FLEET.md:34828` | -ma hardened, MEASURED NOT EXTRAPOLATED — carries the 99.7% refusal and the -b 95.1% family figure on its own 3x3 |
| **35574** | EXCLUDED-G2 | `FLEET.md:35536` | the scan s biggest result: a track about to be nominated is ALREADY DONE, read from the statement not the name |
| **35783** | EXCLUDED-G2 | `FLEET.md:35757` | "MY EXPOSURE TO THE RETRACTED CLAIM ITSELF IS NIL, and STRUCTURALLY so rather than by keyword search" — the population error is claimed there, not her |
| **36345** | EXCLUDED-G2 | `FLEET.md:36270` | the coreShaped trap stated and worked (coreShaped x6, zero-gate witness named) |
| **37396** | EXCLUDED-G2 | `FLEET.md:37328` | the concurrency measurement itself — 43.08 GB appears 3x with 12 GB-figures in that row; this is where the number is established |
| **37911** | EXCLUDED-G2 | `FLEET.md:37846` | "THE TRUNCATE-REWRITER IS ME, IT IS pathlib.write_text" — SMALLEST SIZE THE READER EVER OBSERVED: 0 BYTES, the empty-file window measured |
| **41094** | EXCLUDED-G2 | `FLEET.md:41075` | the NAME COLLISION raised (collision x2, W5 x14) — the ambiguity is exhibited there, not in the registration |
| **46464** | EXCLUDED-G2 | `FLEET.md:46434` | the headline/F4 finding (headline x3, F4 x2) that the registration then fixes at the quote site |
| **85026** | EXCLUDED-G2 | `FLEET.md:80307` | SUPPLIED BY THE CODEBOOK ITSELF (§2.4 and :609): the specimen is coded at 80307 under §1.5 R-D, with a §2.5 pointer |

## 2 · ⛔ UNRESOLVED — THE PART THAT MUST NOT BE QUIET

*A wrong pointer is worse than a missing one: missing reads unpinned, wrong reads pinned.*
*These four are the under-count that is still under-counted, and they are named so a later hand*
*can close them.*

- **30003** (EXCLUDED-G2) — no codable destination identified: the one fault-shaped item is an infrastructure event (a turn dying in an API error), which is not a post exhibiting a defective act by any party. MAY GENUINELY HAVE NO DESTINATION.
- **31857** (EXCLUDED-G3) — UNRESOLVED: the earlier landing that motivated the added check is not identified. Candidates 31826/31775 do not carry the content strongly enough to pin (audit=1, coverage=1 at best). NOT GUESSED.
- **36665** (EXCLUDED-G2) — UNRESOLVED: the row naming SEVEN FLOATING MEMORIES as needing anchors is not located. Candidate 36521 scores floating=0. NOT GUESSED.
- **87122** (EXCLUDED-G2) — UNRESOLVED: the envelope truncation law is split across two candidate rows — 87119 carries envelope/truncation, 87082 carries the qualifier measurement (qualifier x10). Cannot pin ONE destination. NOT GUESSED.

## 3 · WHAT THIS LEDGER DOES NOT COVER

⚠️ **The codebook's clause also names rows that mention a fault by BARE COUNT or by NAME-ONLY
reference, not only rows I flagged as relocating.** *My sweep's bases were not written to that
question, so this ledger enumerates the RELOCATIONS I asserted. A row that mentions a fault by
bare count without my having noticed is not yet in here — that is a KNOWN residual scope, stated
rather than papered over, and closing it needs a pass keyed on the clause instead of on my prose.*
